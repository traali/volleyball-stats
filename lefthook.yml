pre-commit:
  parallel: false
  commands:
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npx eslint {staged_files}
pre-push:
  parallel: false
  commands:
    monastery-visit:
      run: npm run visit
